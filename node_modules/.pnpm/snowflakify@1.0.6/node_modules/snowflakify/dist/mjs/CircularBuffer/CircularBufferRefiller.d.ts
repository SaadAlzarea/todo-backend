import { SnowflakifyOptions } from '../@types';
import CircularBuffer from './CircularBuffer.js';
export default class CircularBufferRefiller {
    private readonly buffer;
    private readonly generatorOptions;
    private readonly workerCount;
    private workers;
    private readonly isRefillingSAB;
    private readonly isRefillingTa;
    constructor(buffer: CircularBuffer, generatorOptions: SnowflakifyOptions, workerCount?: number);
    refill(): void;
    isRefilling(): boolean;
    private createWorker;
    private cleanup;
}
