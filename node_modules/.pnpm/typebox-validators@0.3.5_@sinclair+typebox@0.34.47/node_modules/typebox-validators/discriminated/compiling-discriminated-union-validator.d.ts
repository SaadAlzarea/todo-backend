import { TObject, TUnion } from '@sinclair/typebox';
import { AbstractCompilingTypedUnionValidator } from '../abstract/abstract-compiling-typed-union-validator';
/**
 * Lazily compiled validator for discriminated-union unions. To improve
 * performance, list the more frequently used types earlier in the union,
 * and list each object's discriminant key first in its properties.
 */
export declare class CompilingDiscriminatedUnionValidator<S extends TUnion<TObject[]>> extends AbstractCompilingTypedUnionValidator<S> {
    #private;
    /** @inheritdoc */
    constructor(schema: Readonly<S>);
    protected compiledFindSchemaMemberIndex(value: Readonly<unknown>): number | null;
}
//# sourceMappingURL=compiling-discriminated-union-validator.d.ts.map